/** One apparent receiving surface, just behind the flat letters and in front of the patterned parallax layers. */
export const SHADOW_RECEIVER_Z = -0.06;
/** Objects on this layer cast in the glass projection as opaque casters, drawn under its lamp with an override. */
export const SHADOW_CASTER_LAYER = 2;
/** Apparent slab thickness of the title's glass, used by shadow marching and refraction. */
export const GLASS_DEPTH = 1.2;
