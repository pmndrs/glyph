---
title: Errors
description: TextFrameError names its cause and the span that caused it; where errors throw, where they are retained, and what the renderer keeps showing.
nav: 28
sourcecode: packages/glyph/src/three/frame-error.ts
---

<Intro>
  Malformed input fails where it enters — construction, `set()`, a query — with the offending value in the stack.
  A rejection the engine reports during `glyph.shape()` is retained on the text and its root and forwarded to
  `onError`; that root keeps its last accepted draw while every other root's publication proceeds.
</Intro>

```ts
import { TextFrameError } from '@pmndrs/glyph/three';

label.onError = (error) => {
  if (error instanceof TextFrameError) console.warn(error.rejection.cause, error.rejection);
};
```

<iframe
  src="/examples/errors/"
  title="Provoke each TextFrameError cause"
  loading="lazy"
  style={{ width: '100%', aspectRatio: '16 / 9', border: 0, borderRadius: '0.5rem' }}
/>

## Causes

| `rejection.cause` | Meaning | You fix it by |
| --- | --- | --- |
| `span-range` | inverted or out-of-range span | authoring valid offsets |
| `cluster-boundary` | a style split a grapheme cluster | (resolved constructively at `Text`; reaching the engine is a defect) |
| `span-overlap` | partial overlap between spans | nesting or disjoint ranges |
| `paragraph-root` | missing paragraph root style | supplying `style` |
| `font-stack-missing` | an unregistered stack | loading and binding the font first |
| `font-metrics-missing` | a font with no metrics | re-baking |
| `capacity` | request/result buffers too small; carries `requiredRequestBytes`, `requiredResultBytes` | raising `commands` capacities on the config |
| `engine` | an internal invariant; names nothing | filing it |

{/* prose: `rejection.subject` is `{ kind: 'span', text, index, span }`, `{ kind: 'paragraph', text }`, or
`{ kind: 'unattributed' }`. Six causes are caller-actionable and named; `engine` keeps every internal invariant. */}

## Where errors surface

| Entry | Behaviour |
| --- | --- |
| `createText`, `set()`, `text.font =` on an unloaded face | throw synchronously; desired state is left as it was |
| `glyph.shape()` | per root: retained on `text.error` and `root.error`, `onError` called; other roots still publish |
| `measure()` / `glyphs()` | throw for invalid constraints at the call |
| a font load | rejects the selection's `load()` promise with `FontLoadError`; the record is evicted so a retry is a new operation |

## What the renderer keeps

{/* prose: a renderer-side failure leaves the last accepted draw live and `commitState()` reports
`{ status: 'failed', error }`; unchanged roots do not re-enter the batch; assigning material or other renderer-relevant
state requests a fresh publication. A malformed emitted plan is an engine defect, never a recovery path. */}

## Fixed capacity is not an error

{/* prose: exceeding a `fixed` root capacity keeps the last draw and `commitState()` `pending`; `measure()` still
answers for desired text; the decision is retried on every shape. Link Text, roots, and TextGroup → Capacity. */}

## React

{/* prose: `onError` on `<Text>` and `<TextGroup>`; `GlyphProvider errorFallback` catches only `FontLoadError` from
its own `fontFaces` and rethrows everything else to your boundary. */}
