/** The glyph under the microscope; an ampersand is all curves. */
export const GLYPH = '&';
export const FONT_SIZE = 5.4;
/** Slug divides a glyph into this many bands each way. */
export const BANDS = 16;
/** Seconds per band as the highlight sweeps. */
export const SWEEP = 0.45;
