export const metadata = Object.freeze({ id: '2026-09-01-glyph-handles' });

// Handle selection is an ownership decision and cannot be inferred safely from a constructor call.
export function transform() {}
