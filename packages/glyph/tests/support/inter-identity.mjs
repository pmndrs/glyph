import golden from '../fixtures/inter-bitmap-v0.json' with { type: 'json' };

export const interSourceFingerprint = golden.source.fingerprint;
export const interShapingFingerprint = golden.source.shapingFingerprint;
