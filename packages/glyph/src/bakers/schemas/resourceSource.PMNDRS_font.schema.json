{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "PMNDRS font resource source",
  "description": "Page payloads are always embedded in the artifact that declares them; a page is never a separate file.",
  "type": "object",
  "properties": {
    "type": { "type": "string", "enum": ["bufferView"] },
    "bufferView": { "type": "integer", "minimum": 0 }
  },
  "required": ["type", "bufferView"],
  "additionalProperties": false
}
